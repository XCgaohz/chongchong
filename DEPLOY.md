# 🚀 微信小游戏部署手册

> 本手册分四步：前三步需要你本人操作（涉及微信账号身份），第四步起可以全部交给 ZCode 自动化。

---

## 第 0 步：注册小程序 AppID（一次性，约 5 分钟）

1. 打开 [mp.weixin.qq.com](https://mp.weixin.qq.com) → 立即注册 → 选**小程序**
2. 用一个**没注册过公众号/小程序的邮箱**作为登录邮箱
3. 主体类型选择：
   - **个人主体**：免费、用身份证+微信扫码即可。⚠️ 但游戏类目对个人不开放 → 只能把项目作为**工具/演示类**在开发者工具里使用，无法正式发布为"小游戏"
   - **个体户/企业主体**：可申请游戏类目。正式运营需要 additionally：《计算机软件著作权》、游戏自审自查报告、**版号**（境内出版审批，门槛较高）
4. 注册完成后：登录 → **管理 → 开发管理 → 开发设置** → 复制 **AppID**（wx 开头的一串）

> 💡 现实策略：先用个人免费账号拿 AppID 走**体验版**分发给朋友（见第 4 步）；将来想正式运营再升级主体。

---

## 第 1 步：开发者工具登录 + 开启服务端口（约 2 分钟）

1. 打开微信开发者工具（已安装：`C:\Program Files (x86)\Tencent\微信web开发者工具`）
2. 用**微信扫码**登录（你的微信会绑定为开发者）
3. 右上角 **设置（齿轮）→ 安全设置 → 服务端口 → 开启**
   - 这一步是让命令行可以驱动工具，ZCode 之后能自动完成"导入/上传/预览"

✅ 完成后告诉我一声，以下自动化我就能接管：
```
cli.bat open --project E:\game\chongchong      # 自动导入项目
cli.bat upload --project E:\game\chongchong -v 0.4.0 -d "版本描述"   # 上传为开发版本
cli.bat preview --project E:\game\chongchong   # 生成真机预览二维码
```

---

## 第 2 步：填入 AppID

把你复制的 AppID 发给 ZCode（或自己改）：

`project.config.json` 里：
```json
"appid": "wx1234567890abcdef"   // 替换 touristappid
```

---

## 第 3 步：模拟器验证

导入项目后模拟器会自动编译运行：
- 若报"ES6 转码"相关错误：详情 → 本地设置 → 勾选 **ES6 转 ES5**（本项目 `project.config.json` 已默认开启）
- 单机闯关、热座对战在模拟器直接可玩
- 手机效果：工具栏点「预览」→ 生成二维码 → 微信扫码真机试玩

---

## 第 4 步：发布形态（三选一）

### 4a. 体验版（推荐，门槛最低）
1. 开发者工具右上角 **上传** → 填版本号（如 0.4.0）和备注
2. 网页版 mp.weixin.qq.com → **版本管理** → 开发版本 → **选为体验版**
3. **成员管理 → 体验成员** → 添加朋友的微信号（上限约 90 人）
4. 朋友扫体验版二维码即可游玩——**无需审核**

### 4b. 好友在线对战（需云开发）
1. 开发者工具顶部 **「云开发」** 按钮 → 开通（每个账号可免费创建一个云环境）
2. 云开发控制台 → **数据库** → 创建集合 **`cc_rooms`**（权限选"仅创建者可读写"即可）
3. 左侧 **云函数** 列表中，逐个右键以下目录 → **上传并部署：云端安装依赖**：
   - `cloudfunctions/room-create`
   - `cloudfunctions/room-join`
   - `cloudfunctions/room-sync`
4. 完成。游戏内"好友对战"即全网络可用（建房 → 分享 → 好友输房号进房）

### 4c. 正式发布（面向公众运营）
需要：企业主体 + 游戏类目 + 软著 + 自审自查报告 + **版号**。在 mp.weixin.qq.com 提交审核。个人开发者通常止步于 4a 体验版。

---

## 常见问题

| 问题 | 处理 |
|---|---|
| CLI 报"服务端口已关闭" | 第 1 步第 3 小点没做；或改完端口后需重启开发者工具 |
| 模拟器白屏 | 详情 → 本地设置 → 勾选"不校验合法域名"和"ES6 转 ES5" |
| 真机预览白屏/报错 | 把 vConsole 打开发截图给 ZCode（详情→本地设置→打开调试） |
| 云函数部署失败 | 检查是否已开通云开发并选择了环境；`wx-server-sdk` 走云端安装 |
| 朋友扫码提示"无权限" | 未被添加为**体验成员**（第 4a 步第 3 点） |

---

## 当前版本状态

- ✅ 代码包就绪：`project.config.json`（touristappid 游客模式）/ `game.json` / 全部源码
- ✅ 包完整性校验通过：`node web/check-package.mjs`
- ✅ 云函数源码就绪：`cloudfunctions/`（3 个）
- ⏳ 待你完成：AppID、开发者工具登录、服务端口
